{
  "description": "Runs slopcheck on files Claude writes and surfaces high-severity UX and copy slop (four rules; no visual signature is high severity). Advisory, never blocks.",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/slopcheck-hook.sh\""
          }
        ]
      }
    ]
  }
}
